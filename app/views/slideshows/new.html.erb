<h1>New slideshow <%= current_user.name %></h1>

<%= render 'form' %>

<%= link_to 'Back', slideshows_path %>

<% @images.each do |image| %>
	<img src="<%= image.image %>" data-image-id="<%= image.id %>" class="img-thumb">
<% end %>
