<%= form_for(@photo_library) do |f| %>
  <% if @photo_library.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@photo_library.errors.count, "error") %> prohibited this photo_library from being saved:</h2>

      <ul>
      <% @photo_library.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :photograph_id %><br />
    <%= f.number_field :photograph_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
